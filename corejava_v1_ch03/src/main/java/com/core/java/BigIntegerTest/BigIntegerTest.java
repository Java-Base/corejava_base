package com.core.java.BigIntegerTest;

/**
 * This program uses big numbers to compute the odds of winning the grand prize in a lottery
 *
 * @author 	Lian
 * @time	2016年8月2日
 */
public class BigIntegerTest {

	public static void main(String[] args) {
		String greeting = "hello";
		
		// the length of code unit
		System.out.println("the length of code unit: " + greeting.length());
		
		// the length of code point
		System.out.println("the length of code point: " + greeting.codePointCount(0, greeting.length()));
	}
}
